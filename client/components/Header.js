"use strict";

import React from 'react';

export default function Header (props) {
    return (
        <header className="text-center bg-primary">
            <h2>Просто чат</h2>
        </header>
    );
}